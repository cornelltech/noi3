$("#survey-container").html("<%= escape_javascript(render partial: 'teach_menu'  ) %>"); 

<!-- for adding expertise only through profile -->
$(".main-content").html("<%= escape_javascript(render partial: 'teach_menu'  ) %>"); 