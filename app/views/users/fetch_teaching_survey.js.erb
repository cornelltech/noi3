if (window.location.pathname == "/") {
  $("#secondary-content").html("<%= escape_javascript(render partial: 'teach_survey', locals: { survey: @survey }) %>"); 
} else {
  $(".main-content__container").html("<%= escape_javascript(render partial: 'teach_survey', locals: { survey: @survey }) %>"); 
}
